#include<vector>
#include<map>

using namespace std;

class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        int l, r = 0, nums. - 1;
        map<int, int> m;
        while(l < r){
            if(map.)
        }
    }
};